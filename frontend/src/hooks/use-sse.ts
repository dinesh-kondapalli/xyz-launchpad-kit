"use client";

// SSE feed requires a backend server. Currently disabled — tokens are fetched
// directly from the contract via REST polling. This hook is a no-op until a
// backend indexer is deployed.

export function useSSEFeed() {
  // no-op
}
